name: environment

on:
  push:
    branches:
    - main
  workflow_dispatch:


jobs:
  job1:
    environment: main
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ env.ENV_TYPE }}"

  job2:
    environment: prod
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ env.ENV_TYPE }}"